---
description: Deploy the application to Vercel (production)
---

This workflow triggers a production deployment to Vercel.

1. Ensure all changes are saved and built locally (optional but recommended).
// turbo
2. Run the Vercel production deployment command:
```powershell
npx.cmd vercel --prod --yes
```
3. Verify the deployment URL provided in the terminal output.
